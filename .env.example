# Server configuration
HOST=0.0.0.0
PORT=8000

# Path to the MCP server module (relative or absolute)
MCP_SERVER_PATH=./mcp

# CORS settings (optional - comma separated origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
